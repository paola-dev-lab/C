# 🧩 Exercice — Comparaison `for` vs `while` (dossiers séparés)

🎯 Objectif : comparer les boucles `for` et `while` en produisant **le même résultat logique**, dans **deux implémentations distinctes**

---

## 🎯 Objectifs pédagogiques

Cet exercice a pour but de :

- comprendre que les boucles `for` et `while` permettent d’implémenter **la même logique**
- apprendre à choisir la boucle adaptée selon la consigne
- distinguer clairement :
  - l’**indice** (parcours du tableau)
  - le **compteur** (nombre de valeurs affichées)
- structurer un rendu propre, lisible et conforme aux attentes de 42

- ⚠️  
- le dossier `for/` doit contenir **uniquement** une version avec une boucle `for`
- le dossier `while/` doit contenir **uniquement** une version avec une boucle `while`

---

## 🔎 Contexte

Les deux programmes doivent :

- parcourir un tableau d’entiers
- afficher uniquement les nombres pairs
- afficher le nombre total de valeurs paires trouvées

👉 **Le résultat logique (output) doit être identique dans les deux versions.**

---

## 📄 Consigne commune aux deux programmes

Chaque programme doit :

1. Déclarer un tableau de **10 entiers** initialisé avec des valeurs de ton choix
2. Calculer dynamiquement la taille du tableau
3. Parcourir le tableau à l’aide d’un **indice**
4. Afficher uniquement les valeurs **paires**
5. Utiliser un **compteur distinct** pour compter les valeurs paires affichées
6. Afficher :
   - le nombre total de valeurs du tableau
   - le nombre total de valeurs paires affichées
7. Compiler sans warning

---
À la fin de l’exercice, être capable d’expliquer :

- où se trouvent :
  - l’initialisation
  - la condition
  - l’incrémentation
  dans chaque version
- pourquoi le résultat est identique
- dans quel cas utiliser `for` ou `while`
- pourquoi le respect de la consigne est crucial à 42

