# 🧩 Exercice — Manipulation des types numériques (long, unsigned, float)

## 🎯 Objectif

Approfondir la manipulation des types numériques en C en effectuant des opérations sur :

- un `long`
- un `unsigned`
- un `float`

L’exercice renforce la maîtrise des formats `printf()` et montre comment les types réagissent lors de calculs.

---

## 📝 Consigne

1. Déclarer trois variables :
   - `long a = 5000L;`
   - `unsigned b = 200U;`
   - `float c = 4.5F;`

2. Afficher leurs valeurs **initiales**.

3. Effectuer les modifications :
   - ajouter **1000** à `a`
   - multiplier `b` par **3**
   - diviser `c` par **2**

4. Afficher les **nouvelles valeurs** après les opérations.
