# 🧩 Exercice — Déclaration et affichage de types numériques (long, unsigned, float)

## 🎯 Objectif

Apprendre à déclarer différents types numériques en C et à les afficher avec les bons spécificateurs de `printf()`.

Cet exercice introduit trois types fondamentaux :

- `long` → entier long
- `unsigned` → entier positif (sans signe)
- `float` → nombre décimal simple précision

---

## 📝 Consigne

1. Déclarer trois variables :
   - `long x = 100L;`
   - `unsigned y = 17U;`
   - `float z = 3.0F;`

2. Afficher leurs valeurs en utilisant les bons formats d'affichage :
   - `%ld` pour `long`
   - `%u` pour `unsigned`
   - `%f` pour `float`

3. Compiler et exécuter le programme.



