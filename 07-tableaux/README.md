# 📁 Tableaux — Exercices en C

Ce dossier regroupe des exercices d’entraînement sur les **tableaux en C**, en lien avec les **boucles `for` et `while`**.

L’objectif est de comprendre comment parcourir un tableau, filtrer des valeurs et comparer différentes structures de boucle tout en produisant le même résultat.

---

## 🧩 Exercice 1 — Parcourir un tableau avec `for`

📁 `parcourir_tableau_for/`

- Parcours d’un tableau d’entiers
- Utilisation d’une boucle `for`
- Affichage des valeurs paires
- Utilisation d’un indice pour parcourir le tableau
- Utilisation d’un compteur pour compter les valeurs affichées

---

## 🧩 Exercice 2 — Comparaison `for` vs `while`

📁 `tableaux_for_while/`

Deux implémentations distinctes produisant **le même résultat** : voir README.md à l'intérieur du dossier.

## 🛠️ Compilation

```bash
gcc fichier.c
./a.out

- une version avec une boucle `for`
- une version avec une boucle `while`
