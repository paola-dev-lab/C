# 🧩 Exercice — Bloc 2 (Pré-Piscine 42) : C Basics + Workflow Git

🎯 Objectif : écrire, compiler, exécuter et expliquer un programme C simple, en appliquant un workflow Git propre.

---

## ✅ Compétences visées

### C
- Structure minimale d’un programme C (`main`, `#include`, `return`)
- `printf` et formats simples (`%d`)
- Variables et types `int`
- Conditions (`if / else`)
- Boucles (`while`)
- Lire et corriger une erreur de compilation (ex : point-virgule manquant)

### Git
- Comprendre et utiliser `git clone` (nouveau)
- Utiliser `git status`, `git add`, `git commit`, `git push`, `git pull`
- Organiser un dossier d’exercice propre dans le repo